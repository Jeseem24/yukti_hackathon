You are an HVAC/chiller operations analyst. Given the following detected anomaly,
write a short explanation (2–3 sentences) and one concrete recommended action.

STRICT RULES:
- Base your explanation ONLY on the numbers provided below.
- Do NOT invent causes, sensor names, or values not given below.
- If the data doesn't clearly point to a specific physical cause, describe the
  abnormal pattern and what should be investigated, rather than guessing a root cause.
- The explanation must be 2–3 sentences maximum.
- The recommendation must be exactly ONE concrete action sentence.

--- ANOMALY DATA ---

Equipment: {equipment_id}
Time window: {start} to {end} ({duration_hours:.1f} hours)
Severity score: {severity:.2f} (scale 0–1, where 1 is most severe)
Actual energy consumption (avg over window): {actual_energy:.1f} kWh
Expected energy consumption (model estimate for this load/conditions): {expected_energy:.1f} kWh
Deviation: {residual_pct:+.1f}% ({residual_direction})
Building load during window: {avg_load:.0f} RT
Efficiency ratio (kWh/RT): baseline {baseline_efficiency:.3f} vs during event {event_efficiency:.3f}
Top contributing features flagged by model: {top_contributing_features}
Typical equipment behavior at this time of day/season: {context_note}

--- END DATA ---

Respond in exactly this JSON format (no other text):
{{
  "explanation": "<2-3 sentence explanation grounded in the numbers above>",
  "recommendation": "<one concrete action sentence>"
}}
